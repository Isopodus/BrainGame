export const styles = ({ sizes }) => ({
  form: {
    flex: 1,
    justifyContent: "center",
    alignItems: "stretch",
    paddingHorizontal: "5%",
  },
  input: {
    marginBottom: sizes.scale(20),
  },
  button: {
    marginTop: sizes.scale(30),
    marginBottom: sizes.scale(10),
    paddingVertical: sizes.scale(19),
  },
});

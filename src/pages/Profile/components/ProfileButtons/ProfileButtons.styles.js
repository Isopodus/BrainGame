export const styles = ({ colors, sizes, fonts }) => ({
  container: {
    marginTop: sizes.scale(40),
    alignItems: "stretch",
    paddingHorizontal: "2%",
  },
  button: {
    marginBottom: sizes.scale(20),
  },
});
